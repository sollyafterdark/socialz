DB-01 pre-migration report — actual output
Run against the live socialz-postgres database (container: socialz-postgres,
db: postiz-db) on 2026-09-15, via:

  docker exec -i socialz-postgres psql -U postiz-user -d postiz-db \
    < libraries/nestjs-libraries/src/database/prisma/scripts/db-01-pre-migration-report.sql

=== DB-01 report: per-workspace OWNER fallback resolution ===
 organizationId | organizationName | member_count | superadmin_count | admin_count | fallback_rule | auto_promoted_user_id | auto_promoted_email | auto_promoted_name | reason
----------------+------------------+--------------+------------------+-------------+---------------+------------------------+----------------------+---------------------+--------
(0 rows)


=== DB-01 report: current SUPERADMIN rows (decide User.isSuperAdmin by hand) ===
 organizationId | organizationName | userId | email | name | became_superadmin_at | already_platform_admin
----------------+------------------+--------+-------+------+-----------------------+-------------------------
(0 rows)

Interpretation: the live database currently has zero rows in Organization,
User, and UserOrganization (confirmed directly: `SELECT count(*) FROM
"User"` / `"Organization"` / `"UserOrganization"` all return 0 as of this
run). There is nothing to migrate and nothing to fall back on yet — no
workspace hits any of the four rules because no workspace exists. This is
the actual current state, not a query bug; re-run this script closer to
the real cutover once the app has real workspaces/users, since the owner
mentioned the app "may have at least the owner's own account in it by
now" — as of this run, it does not yet.
