export const BRAND_NAME = 'Socialz';

// Registered legal entity behind the product; shown wherever the app needs
// to identify the business operating it (e.g. settings/legal info).
export const LEGAL_NAME = 'MP Solara';

// Intentionally empty until the ABN is available. Consumers must not render
// anything for this when it's blank.
export const ABN = '';
